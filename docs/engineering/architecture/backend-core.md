# Backend Core

The current backend core is centered on `services/agent-orchestrator`, which coordinates runtime preparation, tool execution, policy checks, checkpointing, memory updates, and audit recording.
