# Local Development

1. Install dependencies with `pnpm install`.
2. Run the API gateway with `pnpm dev`.
3. Create a mission through the HTTP API.
4. Inspect memory and audit outputs through the in-memory services.
